title: CSLab Courses
description: Course pages for CSLab
theme: minima

markdown: kramdown

collections:
  courses:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: default
